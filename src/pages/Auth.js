import AuthForm from "../components/Auth/AuthForm";
import Template from "../components/UI/Template/Template";

const Auth = () => {
    return (
        <Template>
            <AuthForm />
        </Template>
    );
}

export default Auth;